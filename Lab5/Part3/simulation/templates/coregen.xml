<?xml version="1.0" encoding="UTF-8"?>
<RootFolder label="COREGEN" treetype="folder" language="COREGEN">
	<Folder label="VERILOG Component Instantiation" treetype="folder">
		<Template label="DMem" treetype="template">
 
 
// The following must be inserted into your Verilog file for this
// core to be instantiated. Change the instance name and port connections
// (in parentheses) to your own signal names.
 
DMem YourInstanceName (
    .addra(addra), // Bus [7 : 0] 
    .addrb(addrb), // Bus [7 : 0] 
    .clka(clka),
    .clkb(clkb),
    .dina(dina), // Bus [63 : 0] 
    .dinb(dinb), // Bus [63 : 0] 
    .douta(douta), // Bus [63 : 0] 
    .doutb(doutb), // Bus [63 : 0] 
    .wea(wea),
    .web(web));

 
		</Template>
		<Template label="IMem" treetype="template">
 
 
// The following must be inserted into your Verilog file for this
// core to be instantiated. Change the instance name and port connections
// (in parentheses) to your own signal names.
 
IMem YourInstanceName (
    .addr(addr), // Bus [8 : 0] 
    .clk(clk),
    .din(din), // Bus [31 : 0] 
    .dout(dout), // Bus [31 : 0] 
    .we(we));

 
		</Template>
	</Folder>
	<Folder label="VHDL Component Instantiation" treetype="folder">
		<Template label="DMem" treetype="template">
 
 
-- The following code must appear in the VHDL architecture header:
 
component DMem
    port (
    addra: IN std_logic_VECTOR(7 downto 0);
    addrb: IN std_logic_VECTOR(7 downto 0);
    clka: IN std_logic;
    clkb: IN std_logic;
    dina: IN std_logic_VECTOR(63 downto 0);
    dinb: IN std_logic_VECTOR(63 downto 0);
    douta: OUT std_logic_VECTOR(63 downto 0);
    doutb: OUT std_logic_VECTOR(63 downto 0);
    wea: IN std_logic;
    web: IN std_logic);
end component;


 
-------------------------------------------------------------
 
-- The following code must appear in the VHDL architecture body.
-- Substitute your own instance name and net names.
 
your_instance_name : DMem
        port map (
            addra =&gt; addra,
            addrb =&gt; addrb,
            clka =&gt; clka,
            clkb =&gt; clkb,
            dina =&gt; dina,
            dinb =&gt; dinb,
            douta =&gt; douta,
            doutb =&gt; doutb,
            wea =&gt; wea,
            web =&gt; web);
 
		</Template>
		<Template label="IMem" treetype="template">
 
 
-- The following code must appear in the VHDL architecture header:
 
component IMem
    port (
    addr: IN std_logic_VECTOR(8 downto 0);
    clk: IN std_logic;
    din: IN std_logic_VECTOR(31 downto 0);
    dout: OUT std_logic_VECTOR(31 downto 0);
    we: IN std_logic);
end component;


 
-------------------------------------------------------------
 
-- The following code must appear in the VHDL architecture body.
-- Substitute your own instance name and net names.
 
your_instance_name : IMem
        port map (
            addr =&gt; addr,
            clk =&gt; clk,
            din =&gt; din,
            dout =&gt; dout,
            we =&gt; we);
 
		</Template>
	</Folder>
</RootFolder>
